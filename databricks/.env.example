# Databricks credentials — copy this file to `.env` and fill in your values
# Get these from: Databricks workspace → User Settings → Developer → Access Tokens

DATABRICKS_HOST=https://<workspace-id>.azuredatabricks.net
DATABRICKS_TOKEN=dapi...

# SQL Warehouse HTTP path — required for backend to query Delta tables
# Get from: SQL Warehouses → your warehouse → Connection Details → HTTP Path
DATABRICKS_SQL_WAREHOUSE_PATH=/sql/1.0/warehouses/<warehouse-id>

# Kaggle credentials — get from https://www.kaggle.com/settings → API → Create New Token
# This downloads kaggle.json; paste the values here
KAGGLE_USERNAME=your_kaggle_username
KAGGLE_KEY=your_kaggle_api_key

# Optional: override the default CSV path if you already have the file
# CSV_PATH=C:\path\to\stock_details_5_years.csv
